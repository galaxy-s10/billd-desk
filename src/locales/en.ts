import app from '@/locales/en/app';
import common from '@/locales/en/common';
import dashboard from '@/locales/en/dashboard';
import deployment from '@/locales/en/deployment';
import live from '@/locales/en/live';
import remote from '@/locales/en/remote';
import router from '@/locales/en/router';
import webrtc from '@/locales/en/webrtc';

export default {
  ...app,
  ...common,
  ...dashboard,
  ...deployment,
  ...router,
  ...live,
  ...remote,
  ...webrtc,
};
